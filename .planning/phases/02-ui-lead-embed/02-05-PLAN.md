---
phase: 02-ui-lead-embed
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-04"]
files_modified:
  - landing.html
  - calculator/MANUAL-CHECKLIST.md
autonomous: true
requirements: [INT-02]
must_haves:
  truths:
    - "С лендинга заметной кнопкой «Получить расчёт» открывается всплывающее окно калькулятора"
    - "Окно закрывается обратно (крестик, фон, Esc), клиент остаётся на лендинге"
    - "Существующие формы и окно «Заказать звонок» лендинга продолжают работать как раньше"
    - "Есть ручной чек-лист приёмки UI-01..LEAD-03 + процедура проверки живой доставки письма"
  artifacts:
    - path: "landing.html"
      provides: "Кнопка «Получить расчёт» открывает окно калькулятора; разметка/стиль/скрипт окна подключены"
      contains: "openCalcModal"
    - path: "calculator/MANUAL-CHECKLIST.md"
      provides: "Ручной чек-лист приёмки этапа + процедура проверки доставки письма (D-02)"
      contains: "MultiCrete@yandex.ru"
  key_links:
    - from: "landing.html кнопка «Получить расчёт»"
      to: "окно калькулятора"
      via: "onclick openCalcModal()"
      pattern: "openCalcModal"
    - from: "landing.html"
      to: "calculator/calculator-ui.js"
      via: "подключение скрипта окна"
      pattern: "calculator-ui|calculator.css"
---

<objective>
Встроить калькулятор в лендинг: перенацелить существующую заметную кнопку «Получить расчёт» (сейчас `goTo('#price')`) на открытие всплывающего окна калькулятора (D-08/INT-02) и подключить разметку/стиль/скрипт окна. Создать ручной чек-лист приёмки этапа с обязательной процедурой проверки живой доставки письма (D-02). Существующие формы и окно «Заказать звонок» лендинга НЕ трогать (Deferred).

Purpose: Это точка входа клиента — без неё калькулятор недоступен с сайта. Закрывает INT-02 и собирает приёмочный чек-лист всего этапа.
Output: правка `landing.html` (кнопка + встраивание окна) и новый `calculator/MANUAL-CHECKLIST.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/02-ui-lead-embed/02-CONTEXT.md
@.planning/phases/02-ui-lead-embed/02-RESEARCH.md
@.planning/phases/02-ui-lead-embed/02-PATTERNS.md

@.planning/phases/02-ui-lead-embed/02-01-SUMMARY.md
@.planning/phases/02-ui-lead-embed/02-04-SUMMARY.md

<interfaces>
<!-- Из планов 01/04: окно калькулятора живёт в calculator/index.html (оверлей #calc-modal-overlay), стиль calculator/calculator.css, логика calculator/calculator-ui.js (ES-модуль) экспонирует openCalcModal()/closeCalcModal()/handleCalcOverlay() для инлайн-onclick. -->

Существующая кнопка-цель в landing.html (строка 548):
  <button class="btn btn-primary" onclick="goTo('#price')"><i data-lucide="calculator"></i> Получить расчёт</button>
  → заменить обработчик на openCalcModal().

Механика окна лендинга (НЕ трогать, отдельный оверлей): #modal-overlay, openModal/closeModal/handleOverlay/submitForm/submitCall (строки 913-926, 932-943). Чекбокс согласия лендинга строка 879 (checked) — НЕ менять.
</interfaces>

<read_first_global>
- landing.html строки 548 (кнопка «Получить расчёт»), 913-951 (механика окна лендинга и скрипты — точка подключения), 9-12 (шрифты)
- calculator/index.html и calculator/calculator-ui.js (что подключать; имена функций окна)
- .planning/phases/02-ui-lead-embed/02-PATTERNS.md раздел «landing.html (ПРАВКА)» (граница: не трогать формы лендинга)
- .planning/phases/02-ui-lead-embed/02-RESEARCH.md «Validation Architecture» / «Wave 0 Gaps» (состав ручного чек-листа + проверка доставки)
</read_first_global>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Встроить окно калькулятора в landing.html и перенацелить кнопку «Получить расчёт»</name>
  <files>landing.html</files>
  <read_first>
    - landing.html строка 548 (кнопка «Получить расчёт» с onclick="goTo('#price')")
    - landing.html строки 913-951 (разметка окна-образца + блок <script> — куда вставлять разметку и подключение скрипта)
    - calculator/index.html (разметка оверлея #calc-modal-overlay — что вставить инлайн)
    - calculator/calculator.css и calculator/calculator-ui.js (что подключить)
    - .planning/phases/02-ui-lead-embed/02-PATTERNS.md раздел «landing.html (ПРАВКА)»
  </read_first>
  <action>
    Правка landing.html. (1) Перенацелить кнопку: на строке 548 заменить onclick="goTo('#price')" на onclick="openCalcModal()" у кнопки «Получить расчёт» (.btn-primary, INT-02, D-08). Текст и иконку кнопки сохранить. Функцию goTo НЕ удалять (используется другими якорями). (2) Встроить окно калькулятора в страницу. Способ — ИНЛАЙН (решение по RESEARCH Open Question 2 / Discretion: проще, общие стили/JS, без CORS; calculator/index.html остаётся самостоятельной страницей по D-07). Конкретно: подключить стиль окна <link rel="stylesheet" href="calculator/calculator.css"> в <head> (после существующих стилей, чтобы тёмные переменные окна не конфликтовали с лендингом — переменные :root совпадают, классы .calc-* новые). Вставить разметку оверлея #calc-modal-overlay (скопировать из calculator/index.html блок .modal-overlay#calc-modal-overlay со всеми полями/контейнерами/формой) перед закрывающим </body>, рядом с существующим #modal-overlay, НЕ заменяя его. Подключить логику окна как ES-модуль: <script type="module" src="calculator/calculator-ui.js"></script> (после lucide). Убедиться, что openCalcModal/closeCalcModal/handleCalcOverlay доступны для инлайн-onclick (в плане 01 они вешаются на window — проверить; если нет, повесить). (3) ГРАНИЦА (Deferred): НЕ трогать существующие формы лендинга submitForm/submitCall (942-943), их разметку (867-882, 913-926), окно «Заказать звонок» (#modal-overlay), чекбокс согласия лендинга (879, checked). lucide.createIcons() после вставки должен по-прежнему отрисовать иконки (если в разметке окна есть data-lucide — вызвать createIcons повторно или убедиться, что вызов идёт после вставки разметки).
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('landing.html','utf8'); if(!h.includes('openCalcModal()')){console.error('button not retargeted'); process.exit(1)} if(h.includes(\"onclick=\\\"goTo('#price')\\\"\")){console.error('old goTo button still present'); process.exit(1)} const need=['calc-modal-overlay','calculator/calculator.css','calculator/calculator-ui.js','type=\"module\"']; const miss=need.filter(t=>!h.includes(t)); if(miss.length){console.error('MISSING:',miss); process.exit(1)} if(!h.includes('submitCall')||!h.includes('id=\"modal-overlay\"')){console.error('landing modal broken'); process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Кнопка «Получить расчёт» (строка ~548) имеет onclick="openCalcModal()"; старого onclick="goTo('#price')" на ней нет.
    - В <head> подключён calculator/calculator.css; перед </body> вставлен оверлей #calc-modal-overlay; подключён <script type="module" src="calculator/calculator-ui.js">.
    - Существующее окно лендинга #modal-overlay и функции submitForm/submitCall сохранены без изменений.
    - Функция goTo сохранена (другие якоря работают).
    - Иконки Lucide продолжают отрисовываться.
  </acceptance_criteria>
  <done>Кнопка открывает окно калькулятора, окно встроено в лендинг, существующая механика лендинга не сломана.</done>
</task>

<task type="auto">
  <name>Задача 2: Ручной чек-лист приёмки calculator/MANUAL-CHECKLIST.md</name>
  <files>calculator/MANUAL-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/02-ui-lead-embed/02-RESEARCH.md «Validation Architecture» (Phase Requirements → Test Map) и «Wave 0 Gaps»
    - .planning/REQUIREMENTS.md (UI-01..UI-06, INT-01/02, LEAD-01..LEAD-03 — формулировки)
    - .planning/phases/02-ui-lead-embed/02-CONTEXT.md (D-02 — обязательная проверка доставки)
  </read_first>
  <action>
    Создать calculator/MANUAL-CHECKLIST.md — ручной чек-лист приёмки этапа 2 (UI/почту автотестами не покрыть). Структура: (1) Авто-проверка движка: `node calculator/engine.selftest.js` зелёный. (2) Чек-лист UI по требованиям, каждый пункт — действие и ожидаемый результат: UI-01 окно открывается кнопкой «Получить расчёт» и закрывается (крестик/фон/Esc); UI-02 поля площадь/толщина/класс/«>80 кг», галочка «>80 кг» меняет текст оговорки, не число; UI-03 «шапка» сметы (trudozatraty, vspomMaterialy, nakladnye, pribyl, nds, transport/komandirovki=0, materialy, ИТОГО, ₽/м²); UI-04 расшифровка классов 1/2/3 и оговорки видны сразу; UI-05 ввод S=0/T=0/без класса даёт подсказку, расчёт не ломается; UI-06 тёмный фирменный стиль + работа на телефоне (DevTools device mode / реальный телефон, окно прокручивается, фон не дёргается); INT-01 модуль calculator/ на чистом HTML/CSS/JS; INT-02 кнопка на лендинге; LEAD-01 цена видна без формы, форма необязательна; LEAD-03 без галочки согласия отправка недоступна, ссылка на /privacy/ работает. (3) ОБЯЗАТЕЛЬНАЯ процедура проверки доставки письма (LEAD-02, D-02): отправить реальную тестовую заявку → проверить, что письмо ПРИШЛО на MultiCrete@yandex.ru, обязательно заглянуть в папку «Спам»; если не дошло — эскалация: проверить From на собственном домене / порт 587 TLS / существование ящика-отправителя → при неудаче переключить endpoint на Web3Forms (D-02). Пометить, что этот пункт — приёмочный гейт перед /gsd-verify-work и требует доступов Reg.ru от владельца. Каждый пункт — чекбокс [ ]. Текст на русском.
  </action>
  <verify>
    <automated>node -e "const m=require('fs').readFileSync('calculator/MANUAL-CHECKLIST.md','utf8'); const need=['UI-01','UI-02','UI-03','UI-04','UI-05','UI-06','INT-01','INT-02','LEAD-01','LEAD-02','LEAD-03','MultiCrete@yandex.ru','Спам','engine.selftest.js','Web3Forms']; const miss=need.filter(t=>!m.includes(t)); if(miss.length){console.error('MISSING:',miss); process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - MANUAL-CHECKLIST.md покрывает все требования UI-01..UI-06, INT-01/02, LEAD-01..LEAD-03 пунктами «действие → ожидаемый результат».
    - Содержит команду авто-проверки движка node calculator/engine.selftest.js.
    - Содержит ОБЯЗАТЕЛЬНУЮ процедуру проверки живой доставки письма на MultiCrete@yandex.ru с проверкой папки «Спам» и эскалацией на Web3Forms (D-02).
    - Помечен приёмочный гейт перед /gsd-verify-work.
  </acceptance_criteria>
  <done>Ручной чек-лист приёмки создан, покрывает все требования этапа и обязательную проверку доставки письма.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| пользователь → лендинг | Клик по кнопке открывает окно. Чисто клиентское поведение; новых серверных границ план не вводит (отправка — план 02/04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | Конфликт id/функций окна калькулятора с окном лендинга | mitigate | Калькулятор использует ОТДЕЛЬНЫЙ оверлей #calc-modal-overlay и переименованные функции openCalcModal/closeCalcModal; окно лендинга #modal-overlay и submitForm/submitCall не изменяются (verify проверяет их наличие). |
| T-02-16 | Information Disclosure | Подключение скриптов/секретов в лендинг | accept | Подключаются только локальные файлы окна (css/js модуля); секретов нет. Web3Forms-ключ в этот этап не зашивается. |
</threat_model>

<verification>
- node-проверки verify обеих задач зелёные.
- Ручной (по MANUAL-CHECKLIST): на лендинге клик «Получить расчёт» → открывается окно калькулятора; закрытие крестиком/фоном/Esc; окно «Заказать звонок» и форма прайса работают как раньше.
- Полный ручной чек-лист + проверка живой доставки письма (на заливке) — приёмочный гейт этапа.
</verification>

<success_criteria>
- Заметная кнопка на лендинге открывает окно калькулятора (INT-02).
- Окно встроено, существующая механика лендинга не сломана.
- Ручной чек-лист приёмки покрывает все требования этапа и обязательную проверку доставки письма (D-02).
</success_criteria>

<output>
После завершения создать `.planning/phases/02-ui-lead-embed/02-05-SUMMARY.md`
</output>
