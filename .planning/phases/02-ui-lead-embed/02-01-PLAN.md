---
phase: 02-ui-lead-embed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - calculator/calculator.css
  - calculator/index.html
  - calculator/calculator-ui.js
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, INT-01]
must_haves:
  truths:
    - "Клиент открывает окно калькулятора кнопкой и закрывает его (крестик, фон, Esc)"
    - "После ввода площадь/толщина/класс показывается «шапка» сметы с ИТОГО и ₽/м²"
    - "Расшифровка классов 1/2/3 и блок оговорок видны сразу вместе с «шапкой» (без контактов)"
    - "Некорректный ввод (S≤0, T≤0, класс не выбран) даёт понятную подсказку и не ломает расчёт"
    - "Окно выполнено в тёмной теме лендинга и прокручивается на телефоне, не дёргая фон"
  artifacts:
    - path: "calculator/calculator.css"
      provides: "Стили окна в тёмной теме «Стальной Vega» + адаптив большого окна"
      contains: "calc-modal"
    - path: "calculator/index.html"
      provides: "Разметка модуля-окна: оверлей, поля, контейнеры результата/оговорок/классов"
      contains: "calc-modal-overlay"
    - path: "calculator/calculator-ui.js"
      provides: "Механика окна, вызов calc(), рендер breakdown, валидация ввода"
      contains: "openCalcModal"
  key_links:
    - from: "calculator/calculator-ui.js"
      to: "calculator/engine.js"
      via: "import { calc } from './engine.js'"
      pattern: "import.*calc.*engine"
    - from: "calculator/calculator-ui.js"
      to: "calculator/params.js"
      via: "import { PARAMS } from './params.js'"
      pattern: "import.*PARAMS.*params"
    - from: "calculator/index.html"
      to: "calculator/calculator-ui.js"
      via: "<script type=\"module\">"
      pattern: "type=.module.*calculator-ui"
---

<objective>
Собрать модуль `calculator/` — всплывающее окно калькулятора в тёмной теме лендинга: поля ввода (площадь/толщина/класс/чекбокс «>80 кг»), валидация, вызов готового движка `calc()` и рендер «шапки» сметы с ИТОГО, ₽/м², оговорками и расшифровкой классов. Всё видно сразу, без контактов (форма заявки и воронка детализации — план 04).

Purpose: Это ядро интерфейса этапа — клиент впервые видит достоверную предварительную цену прямо на сайте. Закрывает UI-01..UI-06 и INT-01.
Output: `calculator/calculator.css`, `calculator/index.html`, `calculator/calculator-ui.js` — работающее окно, открываемое и закрываемое, считающее и отрисовывающее «шапку».
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/02-ui-lead-embed/02-CONTEXT.md
@.planning/phases/02-ui-lead-embed/02-RESEARCH.md
@.planning/phases/02-ui-lead-embed/02-PATTERNS.md
@.planning/phases/02-ui-lead-embed/02-UI-SPEC.md

<interfaces>
<!-- Контракт движка из calculator/engine.js — UI ТОЛЬКО вызывает и форматирует. Не дублировать формулы, не менять engine.js/params.js (сломается engine.selftest.js). -->

calc({ S, T, klass }, PARAMS) -> breakdown
  S — площадь, м²; T — толщина, мм; klass — 1 | 2 | 3.

Поля «шапки» (рендерить СРАЗУ, без контактов — D-09):
  breakdown.trudozatraty, .vspomMaterialy, .nakladnye, .pribyl, .nds,
  .transport (=0), .komandirovki (=0), .materialy, .itogo, .perM2

Оговорки (СРАЗУ — D-11): breakdown.ogovorki — массив из 3 строк, выводить как есть.

Поля детализации (НЕ показывать в этом плане — за контактами, D-10/план 04):
  .stDrobestruika, .stObespylivanie, .stNanesenie, .stElastomer, .sumWorks,
  .stITR, .esn, .sklad, .garantiya, .rabotyItogo, .massElastomer, .cenaElastomer, .perM2mm ...

Расшифровка классов — из PARAMS.koefKlass {1:1.0, 2:1.2, 3:1.4}; тексты — см. UI-SPEC Copywriting.

Чекбокс «>80 кг» НЕ передаётся в calc() и НЕ меняет число (CALC-07). Только меняет показываемый текст оговорки про массу — текст уже в breakdown.ogovorki[0].
</interfaces>

<read_first_global>
Перед стартом исполнитель ОБЯЗАН прочитать (источники истины):
- calculator/engine.js — контракт breakdown (что рисуем)
- calculator/params.js — PARAMS.koefKlass для расшифровки классов
- landing.html строки 15-39 (:root тёмная тема), 104-124 (.btn*), 328-336 (.pf-*), 355-363/410-411 (.modal*), 932-934/951 (механика окна)
- materials/index.html строки 1-15 (каркас статического модуля)
</read_first_global>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Стили окна calculator.css (тёмная тема + адаптив)</name>
  <files>calculator/calculator.css</files>
  <read_first>
    - landing.html строки 15-39 (:root — переменные --blue/--pink/--steel/--scale/--ink/--red/--f-*)
    - landing.html строки 104-124 (.btn / .btn-primary / .btn-secondary)
    - landing.html строки 328-336 (.pf-row / .pf-label / .pf-input / .pf-input:focus / .pf-consent / .pf-submit)
    - landing.html строки 355-363, 410-411 (.modal-overlay / .modal / .modal-close + тёмное переопределение)
    - .planning/phases/02-ui-lead-embed/02-UI-SPEC.md (Color, Typography, Spacing, Interaction Contract)
    - .planning/phases/02-ui-lead-embed/02-RESEARCH.md «Скользкий участок 3» (адаптив окна)
  </read_first>
  <action>
    Создать calculator/calculator.css — самодостаточный CSS тёмной темы «Стальной Vega» (модуль работает и как отдельная страница). Скопировать дословно блок :root из landing.html (15-39): переменные --blue #2F66A5, --pink #EB4D8C, --ink #EAF0F7, --steel #212C3D, --scale #19222F, --line, --muted, --bg, --red #D64550, --f-head Oswald, --f-body Manrope, --f-tech Tektur. Перенести/скопировать классы лендинга: .modal-overlay, .modal, .modal-close, .btn/.btn-primary/.btn-secondary, .pf-row/.pf-label/.pf-input/.pf-input:focus/.pf-consent/.pf-submit (значения как в landing.html, чтобы окно выглядело идентично). НЕ менять вес 700 у .btn (унаследован, D-08). Добавить НОВЫЕ классы окна калькулятора: .calc-modal { background:var(--steel); border-radius:16px; width:100%; max-width:560px; max-height:90dvh; overflow-y:auto; overscroll-behavior:contain; -webkit-overflow-scrolling:touch }. Класс строки «шапки» .calc-row (подпись слева, значение справа, значения font-variant-numeric:tabular-nums, выравнивание по правому краю). Блок ИТОГО .calc-total — крупно (Display 1.75rem), цвет var(--blue) (информация, не действие). Подсказка ошибки .calc-error — цвет var(--red), мелкий. Адаптив: @media (max-width:560px){ .calc-modal-overlay{padding:0} .calc-modal{max-width:100%; height:100dvh; max-height:100dvh; border-radius:0; padding-bottom:env(safe-area-inset-bottom)} }. Правило цветов UI-SPEC: --blue=информация (ИТОГО/focus/числа), --pink=только CTA-кнопки, --red=только ошибки. Уважать prefers-reduced-motion (без агрессивных анимаций). Имена файлов/классов — латиница и дефисы (правило проекта).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('calculator/calculator.css','utf8'); const need=['--steel','--blue','--pink','--red','.calc-modal','.calc-modal-overlay','90dvh','overscroll-behavior','env(safe-area-inset-bottom)','tabular-nums']; const miss=need.filter(t=>!c.includes(t)); if(miss.length){console.error('MISSING:',miss); process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - calculator/calculator.css существует и содержит переменные --steel/--blue/--pink/--red.
    - Содержит классы .calc-modal, .calc-modal-overlay, .calc-row/.calc-total (рендер «шапки»), .calc-error.
    - Содержит адаптив: max-height:90dvh, overflow-y:auto, overscroll-behavior:contain, @media(max-width:560px) с 100dvh и env(safe-area-inset-bottom).
    - Содержит tabular-nums для чисел.
    - ИТОГО окрашено var(--blue) (информация), CTA-кнопки — var(--pink). Никакого подключения assets/styles/base.css (светлая тема запрещена, D-08).
  </acceptance_criteria>
  <done>CSS-файл создан, тёмная тема и адаптивные правила окна присутствуют, проверка проходит.</done>
</task>

<task type="auto">
  <name>Задача 2: Разметка окна calculator/index.html (оверлей, поля, контейнеры результата)</name>
  <files>calculator/index.html</files>
  <read_first>
    - materials/index.html строки 1-15 (каркас head статического модуля)
    - landing.html строки 9-12 (preconnect + Google Fonts), 913-926 (разметка оверлея-образца), 928-930 (Lucide)
    - .planning/phases/02-ui-lead-embed/02-PATTERNS.md раздел «calculator/index.html» (каркас, отдельный id оверлея)
    - .planning/phases/02-ui-lead-embed/02-UI-SPEC.md (Copywriting — точные строки; Interaction Contract — поля)
  </read_first>
  <action>
    Создать calculator/index.html по каркасу materials/index.html, но ВАЖНО: подключать НЕ base.css/catalog.css (светлые), а собственный ./calculator.css (тёмная тема, D-08). В head: meta charset/viewport, title «Калькулятор стоимости покрытия — МультиКрит», meta description, preconnect+Google Fonts (Oswald/Manrope/Tektur, скопировать из landing.html 9-12), <link rel="stylesheet" href="./calculator.css">. В body разметить ОТДЕЛЬНЫЙ оверлей с id="calc-modal-overlay" (не пересекается с #modal-overlay лендинга), onclick="handleCalcOverlay(event)", role="dialog", aria-modal="true", aria-labelledby="calc-title". Внутри .modal.calc-modal: кнопка .modal-close onclick="closeCalcModal()" aria-label="Закрыть"; h2 id="calc-title" «Калькулятор стоимости покрытия»; подзаголовок-пояснение «Предварительный расчёт по площади, толщине и классу сложности. Цена видна сразу, без контактов.». Поля ввода (UI-02): площадь — input type="number" id="calc-S" (м²); толщина — input type="number" id="calc-T" (мм); класс — radio или select id="calc-klass" со значениями 1/2/3; чекбокс id="calc-heavy" «Деталь тяжелее 80 кг». Кнопка расчёта .btn.btn-primary id="calc-run" «Рассчитать стоимость». Контейнеры-приёмники для рендера из JS: id="calc-errors" (подсказки валидации), id="calc-result" (шапка, скрыт до расчёта), id="calc-total" (ИТОГО), id="calc-ogovorki" (оговорки), id="calc-klass-info" (расшифровка классов). Empty state до расчёта: «Введите параметры детали» / «Укажите площадь, толщину и класс сложности — покажем предварительную стоимость и ₽/м².». В конце body: Lucide CDN (как landing.html 928-930) и <script type="module" src="./calculator-ui.js"></script>. Тексты — дословно из UI-SPEC Copywriting. НЕ создавать в этом плане разметку формы заявки/детализации (план 04).
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('calculator/index.html','utf8'); const need=['calculator.css','calc-modal-overlay','role=\"dialog\"','aria-modal','calc-title','calc-S','calc-T','calc-klass','calc-heavy','calc-run','calc-result','calc-total','calc-ogovorki','calc-klass-info','type=\"module\"','calculator-ui.js']; const miss=need.filter(t=>!h.includes(t)); if(h.includes('base.css')){console.error('FORBIDDEN base.css'); process.exit(1)} if(miss.length){console.error('MISSING:',miss); process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - calculator/index.html подключает ./calculator.css и НЕ подключает base.css/catalog.css.
    - Есть оверлей id="calc-modal-overlay" с role="dialog", aria-modal="true", aria-labelledby="calc-title".
    - Есть поля calc-S, calc-T, calc-klass (значения 1/2/3), calc-heavy, кнопка calc-run.
    - Есть контейнеры calc-errors, calc-result, calc-total, calc-ogovorki, calc-klass-info.
    - Подключён <script type="module" src="./calculator-ui.js">.
    - Заголовок окна = «Калькулятор стоимости покрытия», CTA = «Рассчитать стоимость» (тексты из UI-SPEC).
  </acceptance_criteria>
  <done>Разметка окна создана с правильными id, ARIA, тёмным стилем и подключённым ES-модулем UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Логика окна calculator-ui.js (механика, валидация, рендер «шапки»)</name>
  <files>calculator/calculator-ui.js</files>
  <behavior>
    - validateInputs(0, 5, 2) -> { S: 'Укажите площадь больше нуля, м²' } (S≤0 ловится).
    - validateInputs(100, 0, 1) -> { T: 'Укажите толщину больше нуля, мм' } (T≤0 ловится).
    - validateInputs(100, 5, 0) -> { klass: 'Выберите класс сложности' } (класс не выбран ловится).
    - validateInputs(100, 5, 1) -> {} (валидно, пусто).
    - При валидных входах calc({S,T,klass}, PARAMS) вызывается, breakdown.itogo рендерится в #calc-total, числа форматируются (tabular-nums, ₽).
    - Эталон не сломан: node calculator/engine.selftest.js остаётся зелёным (UI не трогает движок).
  </behavior>
  <read_first>
    - calculator/engine.js (контракт breakdown — поля «шапки» и ogovorki)
    - calculator/params.js строки 63-67 (koefKlass для расшифровки классов)
    - landing.html строки 932-934, 951 (openModal/closeModal/handleOverlay + Esc — копируем паттерн)
    - .planning/phases/02-ui-lead-embed/02-PATTERNS.md раздел «calculator-ui.js» (механика, валидация, контракт)
    - .planning/phases/02-ui-lead-embed/02-UI-SPEC.md (Copywriting — тексты ошибок и расшифровки классов; Interaction Contract)
  </read_first>
  <action>
    Создать calculator/calculator-ui.js как ES-модуль. Импорты: import { calc } from './engine.js'; import { PARAMS } from './params.js'. Механика окна (скопировать паттерн landing.html 932-934/951, переименовав под свой оверлей, чтобы не конфликтовать с «Заказать звонок»): openCalcModal() — добавить класс open на #calc-modal-overlay, document.body.style.overflow='hidden', перевести фокус на первое поле (#calc-S); closeCalcModal() — снять класс open, вернуть overflow='', вернуть фокус на кнопку-вызов; handleCalcOverlay(e) — закрыть при клике точно по оверлею; обработчик keydown Esc → closeCalcModal(). Эти функции должны быть доступны из инлайн-onclick разметки (повесить на window или через addEventListener по id). Лёгкий фокус-трап (Tab/Shift+Tab в пределах окна) — желателен, ~10 строк vanilla. Валидация validateInputs(S, T, klass): возвращает объект ошибок — S>0, T>0, klass∈{1,2,3}; тексты ДОСЛОВНО из UI-SPEC: «Укажите площадь больше нуля, м²», «Укажите толщину больше нуля, мм», «Выберите класс сложности». Если ошибки есть — показать подсказки в #calc-errors красным (--red), calc() НЕ вызывать. Обработчик кнопки #calc-run: считать S/T/klass из полей (parseFloat/parseInt), валидировать, при успехе вызвать b = calc({S,T,klass}, PARAMS) и отрисовать «ШАПКУ» в #calc-result: trudozatraty, vspomMaterialy, nakladnye, pribyl, nds, transport(=0), komandirovki(=0), materialy, ИТОГО (b.itogo в #calc-total, крупно/синим), ₽/м² (b.perM2). Числа форматировать через Intl.NumberFormat('ru-RU') (рубли, разряды). Оговорки: вывести b.ogovorki как список в #calc-ogovorki (СРАЗУ, D-11) — НЕ переписывать тексты. Чекбокс #calc-heavy НЕ передавать в calc(); при отметке заменить ТОЛЬКО показываемый текст оговорки про массу (b.ogovorki[0]) — например на пометку, что для тяжёлой детали добавится грузоподъёмное оборудование (число не меняется, CALC-07). Расшифровка классов в #calc-klass-info из PARAMS.koefKlass + тексты UI-SPEC: Класс 1 — «простое тело вращения» (×1.0); Класс 2 — «сложное тело вращения (импеллер/винт)» (×1.2); Класс 3 — «деталь с закрываемыми зонами (обклейка/маскировка)» (×1.4). НЕ дублировать формулы/коэффициенты движка — только читать breakdown и PARAMS.koefKlass. НЕ менять engine.js/params.js. Форму заявки/воронку детализации/отправку в этом плане НЕ реализовывать (план 04). Чтобы validateInputs можно было прогнать в Node без DOM — вынести её как отдельную export-функцию (или дублировать чистую функцию в тестовом импорте), но в браузере она же используется.
  </action>
  <verify>
    <automated>node calculator/engine.selftest.js && node --input-type=module -e "import('./calculator/calculator-ui.js').catch(()=>{}); import { calc } from './calculator/engine.js'; import { PARAMS } from './calculator/params.js'; const b=calc({S:100,T:5,klass:1},PARAMS); if(typeof b.itogo!=='number'){process.exit(1)} console.log('engine ok itogo=',b.itogo)"</automated>
  </verify>
  <acceptance_criteria>
    - calculator-ui.js импортирует calc из ./engine.js и PARAMS из ./params.js (один источник чисел).
    - validateInputs ловит S≤0, T≤0, klass∉{1,2,3} с точными текстами UI-SPEC; при валидном входе возвращает пустой объект.
    - При нажатии «Рассчитать стоимость» с валидным вводом рендерится «шапка» с ИТОГО и ₽/м²; некорректный ввод показывает подсказку и не вызывает calc().
    - b.ogovorki и расшифровка классов 1/2/3 выводятся сразу (без контактов).
    - Чекбокс «>80 кг» меняет текст оговорки, но НЕ число (calc() вызывается без него).
    - openCalcModal/closeCalcModal/handleCalcOverlay + Esc работают на #calc-modal-overlay; фокус ставится на первое поле при открытии.
    - node calculator/engine.selftest.js остаётся зелёным (engine.js/params.js не изменены).
  </acceptance_criteria>
  <done>Окно открывается/закрывается, валидация работает, «шапка»+оговорки+классы рендерятся из breakdown, движок не тронут.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| пользователь → браузерный JS | Ввод площади/толщины/класса. Расчёт детерминирован и локален (calc() — чистая функция, без сети). Доверенной границы с сервером в этом плане нет. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Ввод полей калькулятора (S/T/klass) | mitigate | validateInputs отвергает S≤0/T≤0/неверный класс до вызова calc(); нет eval/innerHTML с сырым вводом — числа форматируются через Intl.NumberFormat, не вставляются как HTML. |
| T-02-02 | Information Disclosure | Числа PARAMS/коэффициенты | accept | Коэффициенты публичны по осознанному решению владельца (прозрачный калькулятор, params.js шапка). Это НЕ секрет — правило CLAUDE.md о ключах неприменимо. Секретов/токенов в файлах окна нет. |
| T-02-03 | Denial of Service | Рендер при гигантских S/T | accept | calc() — конечная арифметика без циклов по вводу; крайне больших чисел недостаточно для отказа браузера. Низкий риск, B2B-аудитория. |
</threat_model>

<verification>
- node calculator/engine.selftest.js — зелёный (движок не сломан правками UI).
- Ручной smoke: открыть calculator/index.html → клик «Рассчитать стоимость» при S=100, T=5, класс 1 → видна «шапка» с ИТОГО и ₽/м², оговорки и расшифровка классов.
- Ручной: ввод S=0 → подсказка «Укажите площадь больше нуля, м²», расчёт не выполняется.
- Ручной: окно закрывается крестиком, кликом по фону и Esc; на телефоне (DevTools device mode) окно прокручивается, фон не дёргается.
</verification>

<success_criteria>
- Окно калькулятора открывается и закрывается (UI-01).
- Поля площадь/толщина/класс/«>80 кг» присутствуют; «>80 кг» меняет текст оговорки, не число (UI-02).
- «Шапка» сметы с ИТОГО и ₽/м² рендерится из breakdown (UI-03).
- Расшифровка классов и оговорки видны сразу (UI-04).
- Валидация ловит некорректный ввод с понятной подсказкой (UI-05).
- Окно в фирменной тёмной теме и адаптивно (UI-06).
- Модуль calculator/ собран на чистом HTML/CSS/JS без сборщика (INT-01).
</success_criteria>

<output>
После завершения создать `.planning/phases/02-ui-lead-embed/02-01-SUMMARY.md`
</output>
