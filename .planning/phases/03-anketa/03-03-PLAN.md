---
phase: 03-anketa
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - landing.html
autonomous: false
requirements: [D-08, D-09, D-10, D-11]
must_haves:
  truths:
    - "На landing.html сразу после секции #portfolio (перед «Доверие лидеров») появился блок «С чего начать» с двумя карточками"
    - "Кнопка «Рассчитать стоимость» открывает окно калькулятора через существующий openCalcModal(this)"
    - "Кнопка «Скачать опросной лист» скачивает anketa/oprosnyj-list-multicrete.docx с клиентским именем через <a download>"
    - "Блок оформлен фирменными тёмными классами лендинга и адаптивен на телефоне"
  artifacts:
    - path: "landing.html"
      provides: "Блок «С чего начать» между #portfolio и «Доверие лидеров»"
      contains: "download"
  key_links:
    - from: "landing.html (кнопка калькулятора)"
      to: "openCalcModal(this)"
      via: "onclick существующего вызова окна калькулятора этапа 2"
      pattern: "openCalcModal\\("
    - from: "landing.html (кнопка опросного листа)"
      to: "anketa/oprosnyj-list-multicrete.docx"
      via: "<a href download>"
      pattern: "download="
---

<objective>
Добавить на `landing.html` блок «С чего начать» с двумя карточками-кнопками: «Онлайн-калькулятор» (переиспользует `openCalcModal(this)`) и «Опросной лист» (скачивает DOCX прямой ссылкой). Тексты дословно из D-11, размещение по D-08, стиль по D-10, поведение по D-09.

Purpose: клиент, посмотрев примеры работ (#portfolio), сразу видит два способа начать — посчитать самому или прислать данные на подбор (логика владельца, D-08).
Output: правка `landing.html` — новая секция между `#portfolio` и «Доверие лидеров».
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-anketa/03-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Точка встраивания и фирменные паттерны — из landing.html. Исполнитель вставляет между этими секциями. -->

Точка вставки — между закрывающим `</section>` секции `#portfolio` (строка ~833) и открывающим комментарием `<!-- ══ CLIENTS ══ -->` секции «Доверие лидеров» (строка ~835).

Существующий вызов калькулятора (строка 551, переиспользуем ЕГО):
`<button class="btn btn-primary" onclick="openCalcModal(this)"><i data-lucide="calculator"></i> Получить расчёт</button>`

Фирменный каркас секции (аналог, строки 822-833):
`<section class="section" id="...">`
`  <div class="wrap">`
`    <span class="sec-label">// Метка</span>`
`    <h2 class="sec-title">Заголовок</h2>`
`    <div class="grid-4" ...> ... карточки .card ... </div>`
`  </div>`
`</section>`

Классы: `.section`, `.wrap`, `.sec-label`, `.sec-title`, `.sec-sub` (подзаголовок), `.card`, `.btn`/`.btn-primary`/`.btn-secondary`. Иконки — `<i data-lucide="...">` (lucide уже подключён).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static file | Кнопка скачивания отдаёт статический .docx прямой ссылкой, без сервера |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | landing.html | mitigate | Правка — статический HTML; ссылка ведёт на публичный бланк. Секретов/ключей в разметке нет — проверить перед коммитом (правило CLAUDE.md) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Блок «С чего начать» на landing.html</name>
  <files>landing.html</files>
  <read_first>
    - landing.html (строки 820-855 — точка вставки между #portfolio и «Доверие лидеров»; строка 551 — существующий вызов openCalcModal; строки 64-73 — стили .sec-label/.sec-title)
    - .planning/phases/03-anketa/03-CONTEXT.md (D-08 размещение, D-09 поведение, D-10 стиль, D-11 тексты — дословно)
    - D:\ARHI-AI\ai-architect-potok-1\кабинет\конспекты\multicrete-сайт-бриф.md (правило CLAUDE.md — читать бриф перед изменением страниц сайта; сверить тон и позиционирование блока)
  </read_first>
  <action>
    Вставить новую `<section class="section">` СТРОГО между закрывающим `</section>` секции `#portfolio` и комментарием `<!-- ══ CLIENTS ══ -->` (D-08). Содержимое:
    — `<span class="sec-label">// С чего начать</span>` (D-11 надзаголовок дословно).
    — `<h2 class="sec-title">Посчитайте сами или пришлите данные на подбор</h2>` (D-11 заголовок дословно).
    — Подзаголовок `<p class="sec-sub">Два простых способа начать — выберите удобный.</p>` (D-11 дословно).
    — Две карточки рядом (использовать `.card`, обёрнутые в grid-контейнер аналогично `grid-4`; допустимо два столбца через инлайн-стиль или существующий grid-класс; адаптив на телефоне — карточки в один столбец, D-10).
       Карточка 1 «Онлайн-калькулятор»: иконка `calculator`, заголовок «Онлайн-калькулятор», текст дословно D-11: «За минуту прикиньте предварительную стоимость покрытия по трём параметрам — без звонков и ожидания.», кнопка `<button class="btn btn-primary" onclick="openCalcModal(this)">…Рассчитать стоимость</button>` (D-09 — переиспользовать существующий вызов, не изобретать).
       Карточка 2 «Опросной лист»: иконка `file-text` (или `clipboard-list`), заголовок «Опросной лист», текст дословно D-11: «Скачайте бланк, заполните по вашей детали и пришлите нам — подберём покрытие и подготовим расчёт.», кнопка-ссылка `<a class="btn btn-secondary" href="anketa/oprosnyj-list-multicrete.docx" download="Опросной лист МультиКрит.docx">…Скачать опросной лист</a>` (D-09 прямая ссылка, D-11 текст кнопки; клиентское имя файла кириллицей через атрибут download).
    Стиль — фирменные тёмные классы лендинга (D-10), не base.css. Не трогать существующие секции. Секретов в разметке нет.
  </action>
  <verify>
    <automated>cd d:/Project/multicrete-elastomers && grep -q 'С чего начать' landing.html && grep -q 'openCalcModal(' landing.html && grep -q 'download="Опросной лист' landing.html && grep -q 'anketa/oprosnyj-list-multicrete.docx' landing.html && echo 'block present OK'</automated>
  </verify>
  <acceptance_criteria>
    - landing.html содержит новый блок с `// С чего начать` и заголовком «Посчитайте сами или пришлите данные на подбор».
    - Блок расположен между секцией `#portfolio` и комментарием `<!-- ══ CLIENTS ══ -->` (grep по порядку строк: новый блок после строки с `id="portfolio"` и до `CLIENTS`).
    - Карточка 1 содержит кнопку с `onclick="openCalcModal(this)"` и текст «Рассчитать стоимость».
    - Карточка 2 содержит `<a … href="anketa/oprosnyj-list-multicrete.docx" download="Опросной лист МультиКрит.docx">` и текст «Скачать опросной лист».
    - Тексты карточек совпадают дословно с D-11.
    - Блок использует классы `.section`/`.wrap`/`.sec-label`/`.sec-title`/`.card`/`.btn`. Секретов в разметке нет.
  </acceptance_criteria>
  <done>Блок «С чего начать» с двумя карточками вставлен на landing.html в нужном месте, тексты дословны D-11, кнопки ведут на openCalcModal и на скачивание .docx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Проверка блока и скачивания в браузере (D-09)</name>
  <what-built>
    Блок «С чего начать» на landing.html сразу после «Примеры выполненных работ»: две карточки — «Онлайн-калькулятор» (кнопка «Рассчитать стоимость» → окно калькулятора) и «Опросной лист» (кнопка «Скачать опросной лист» → скачивание DOCX). Фирменный тёмный стиль, адаптив.
  </what-built>
  <how-to-verify>
    1. Открыть `landing.html` в браузере, пролистать до блока сразу ПОСЛЕ «Примеры выполненных работ».
    2. Убедиться, что блок виден, две карточки рядом, тексты и иконки на месте, стиль совпадает с остальным лендингом.
    3. Нажать «Рассчитать стоимость» — должно открыться окно калькулятора (как кнопка «Получить расчёт» в шапке).
    4. Нажать «Скачать опросной лист» — должен скачаться файл с именем «Опросной лист МультиКрит.docx». Открыть скачанный файл — это тот самый бланк.
    5. Сузить окно браузера до ширины телефона — карточки должны перестроиться в один столбец, ничего не «уезжает».
  </how-to-verify>
  <read_first>
    - landing.html (открыть в браузере)
  </read_first>
  <acceptance_criteria>
    - Блок виден сразу после «Примеры выполненных работ», перед «Доверие лидеров».
    - Кнопка «Рассчитать стоимость» открывает окно калькулятора.
    - Кнопка «Скачать опросной лист» скачивает файл с кириллическим именем; файл открывается как корректный бланк.
    - На узком экране карточки адаптивно перестраиваются.
    - Владелец подтвердил словом «approved» либо перечислил правки.
  </acceptance_criteria>
  <resume-signal>Напишите «approved», если блок и скачивание работают, либо перечислите правки по вёрстке/текстам.</resume-signal>
</task>

</tasks>

<verification>
- landing.html содержит блок «С чего начать» в правильном месте (D-08).
- Кнопка калькулятора использует openCalcModal(this) (D-09).
- Кнопка опросного листа — `<a href download>` на anketa/oprosnyj-list-multicrete.docx (D-09), клиентское имя кириллицей.
- Тексты дословны D-11; стиль фирменный (D-10).
- Проверка в браузере пройдена владельцем.
</verification>

<success_criteria>
Клиент на сайте видит блок с двумя способами начать и может открыть калькулятор либо скачать опросной лист. Блок в фирменном стиле, адаптивен, тексты по D-11.
</success_criteria>

<output>
После завершения создать `.planning/phases/03-anketa/03-03-SUMMARY.md`
</output>
