---
phase: 03-anketa
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - anketa/oprosnyj-list-multicrete.docx
autonomous: false
requirements: [D-05, D-06, D-07]
must_haves:
  truths:
    - "Готовый anketa/oprosnyj-list-multicrete.docx собран скриптом из шаблона + тела"
    - "verify-anketa.ps1 проходит зелёным: zip валиден, XML well-formed, колонтитулы байт-в-байт, без BOM, содержимое на месте"
    - "Владелец подтвердил в Word: шапка, логотип, реквизиты на месте, кириллица корректна, таблицы не разъехались (D-07)"
  artifacts:
    - path: "anketa/oprosnyj-list-multicrete.docx"
      provides: "Готовый к заполнению DOCX-бланк на фирменном шаблоне"
      contains: "word/document.xml с телом опросника"
  key_links:
    - from: "anketa/oprosnyj-list-multicrete.docx"
      to: "Входящие/Бланк на опросной лист.docx"
      via: "колонтитулы header1/footer1/media/styles байт-в-байт идентичны шаблону"
      pattern: "cmp -s identical"
    - from: "anketa/oprosnyj-list-multicrete.docx"
      to: "anketa/src/anketa-body.xml"
      via: "тело опросника инъектировано перед <w:sectPr>"
      pattern: "Блок 0"
---

<objective>
Собрать финальный приёмочный DOCX-бланк, прогнать полную проверку и получить ручное подтверждение владельца в Word (D-07).

Purpose: D-05/D-06 — готовый воспроизводимый артефакт на фирменном шаблоне; D-07 — обязательная визуальная приёмка (аккуратность бланка автоматически не проверяется).
Output: `anketa/oprosnyj-list-multicrete.docx` (готовый файл в репозитории, static asset для кнопки скачивания).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-anketa/03-CONTEXT.md
@.planning/phases/03-anketa/03-RESEARCH.md
@.planning/phases/03-anketa/03-VALIDATION.md
@.planning/phases/03-anketa/03-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → static asset | Готовый .docx попадает в репозиторий и раздаётся клиентам |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | anketa/oprosnyj-list-multicrete.docx | mitigate | Файл — публичный бланк; содержит только публичный e-mail MultiCrete@mail.ru (из подвала шаблона). Секретов нет по построению (тело из проверенного anketa-body.xml) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Сборка и полная проверка приёмочного .docx</name>
  <files>anketa/oprosnyj-list-multicrete.docx</files>
  <read_first>
    - anketa/build-anketa.ps1 (скрипт сборки — из Plan 01)
    - anketa/verify-anketa.ps1 (скрипт проверки — из Plan 01)
    - .planning/phases/03-anketa/03-VALIDATION.md (карта проверок — что должно быть зелёным)
  </read_first>
  <action>
    1. Запустить сборку: выполнить `anketa/build-anketa.ps1` из корня репозитория (PowerShell 5.1). Скрипт читает шаблон `Входящие/Бланк на опросной лист.docx` + тело `anketa/src/anketa-body.xml`, пишет `anketa/oprosnyj-list-multicrete.docx`.
    2. Запустить полную проверку: выполнить `anketa/verify-anketa.ps1`. Все пункты должны быть PASS (Word COM smoke может дать WARN, если COM недоступен — тогда приёмка целиком на Task 2).
    3. При любом FAIL — НЕ править .docx руками. Диагностировать причину (экранирование, BOM, пути zip), исправить исходник `anketa/src/anketa-body.xml` или скрипт `build-anketa.ps1`, пересобрать, перепроверить (D-06 — правки только через пересборку).
    4. Проверить имя файла: латиница/дефисы `anketa/oprosnyj-list-multicrete.docx` (правило CLAUDE.md). Шаблон `Входящие/Бланк на опросной лист.docx` НЕ переименовывать и НЕ удалять.
    Секретов в файле нет (только публичный e-mail).
  </action>
  <verify>
    <automated>cd d:/Project/multicrete-elastomers && unzip -t anketa/oprosnyj-list-multicrete.docx | grep -q 'No errors detected' && mkdir -p /tmp/anketa-chk && unzip -o -q anketa/oprosnyj-list-multicrete.docx word/document.xml word/footer1.xml -d /tmp/anketa-chk && head -c 3 /tmp/anketa-chk/word/document.xml | od -An -tx1 | grep -q '3c 3f 78' && grep -q 'MultiCrete@mail.ru' /tmp/anketa-chk/word/footer1.xml && echo 'docx integrity OK'</automated>
  </verify>
  <acceptance_criteria>
    - `unzip -t anketa/oprosnyj-list-multicrete.docx` → «No errors detected».
    - Извлечённый `word/document.xml` начинается байтами `3C 3F 78` (без BOM) и содержит «Блок 0», «Характер воздействия», «УТВЕРЖДАЮ».
    - `cmp -s` header1.xml / footer1.xml / media/image1.jpg / styles.xml / [Content_Types].xml против шаблона → идентичны (проверяет verify-anketa.ps1).
    - Извлечённый `word/footer1.xml` содержит `MultiCrete@mail.ru`.
    - `anketa/verify-anketa.ps1` завершается без FAIL (Word COM — PASS или WARN).
    - Имя файла — латиница/дефисы; шаблон во `Входящие/` не тронут.
  </acceptance_criteria>
  <done>anketa/oprosnyj-list-multicrete.docx собран скриптом, verify-anketa.ps1 зелёный (кроме возможного WARN на COM), колонтитулы байт-в-байт идентичны шаблону.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ручная приёмка бланка владельцем в Word (D-07)</name>
  <what-built>
    Готовый DOCX-бланк `anketa/oprosnyj-list-multicrete.docx`: фирменная шапка (логотип + «Опросной лист») и подвал (ООО «МультиКрит», MultiCrete@mail.ru, телефоны, www.МультиКрит.рф) сохранены из шаблона байт-в-байт; в тело вписано согласованное содержание опросника (блоки 0-6, таблицы, чекбоксы, УТВЕРЖДАЮ, футер-инструкция). Автопроверки целостности/колонтитулов/кодировки — зелёные.
  </what-built>
  <how-to-verify>
    1. Открыть файл `anketa/oprosnyj-list-multicrete.docx` в Microsoft Word (двойной клик).
    2. Убедиться, что Word открывает файл БЕЗ сообщения «повреждён / нужно восстановить».
    3. Проверить ШАПКУ: логотип на месте, надпись «Опросной лист» видна.
    4. Проверить ПОДВАЛ: ООО «МультиКрит», e-mail MultiCrete@mail.ru, телефоны +7-3412-32-41-51 / +7-912-750-26-77, www.МультиКрит.рф.
    5. Проверить ТЕЛО: все блоки 0-6 на месте, кириллица читается корректно (не «кракозябры»), таблицы «Поле | Значение» не разъехались, чекбоксы ☐ отображаются, «УТВЕРЖДАЮ» и финальная инструкция про MultiCrete@mail.ru присутствуют.
    6. Прокрутить весь документ — убедиться, что вёрстка аккуратна (нет пустых страниц, разорванных таблиц, наложений).
  </how-to-verify>
  <read_first>
    - anketa/oprosnyj-list-multicrete.docx (открыть в Word)
    - .planning/research/ANKETA-draft-v1.md (сверить содержание блоков)
  </read_first>
  <acceptance_criteria>
    - Word открыл файл без ошибок восстановления.
    - Шапка (логотип + «Опросной лист») и подвал (реквизиты, MultiCrete@mail.ru) на месте.
    - Кириллица корректна, таблицы не разъехались, все блоки 0-6 присутствуют.
    - Владелец подтвердил приёмку словом «approved» либо перечислил конкретные правки.
  </acceptance_criteria>
  <resume-signal>Напишите «approved», если бланк корректен, либо перечислите, что поправить (правки вносятся в anketa/src/anketa-body.xml → пересборка build-anketa.ps1, не руками в Word).</resume-signal>
</task>

</tasks>

<verification>
- `anketa/oprosnyj-list-multicrete.docx` существует, zip валиден, XML well-formed, без BOM.
- Колонтитулы/логотип/стили байт-в-байт идентичны шаблону (D-05).
- verify-anketa.ps1 проходит без FAIL.
- Владелец подтвердил визуальную приёмку в Word (D-07).
</verification>

<success_criteria>
Готовый к заполнению бланк на фирменном шаблоне лежит в репозитории (static asset), прошёл автопроверки и ручную приёмку владельца. Файл готов к раздаче кнопкой скачивания (Plan 03).
</success_criteria>

<output>
После завершения создать `.planning/phases/03-anketa/03-02-SUMMARY.md`
</output>
