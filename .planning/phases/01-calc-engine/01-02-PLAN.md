---
phase: 01-calc-engine
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - calculator/engine.js
  - calculator/engine.selftest.js
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03, CALC-04, CALC-05, CALC-06, CALC-07]
must_haves:
  truths:
    - "calc({S,T,klass}, PARAMS) возвращает полную разбивку стоимости + список оговорок"
    - "На контрольных примерах ИТОГО и ₽/м² совпадают с эталоном 700 в пределах округления"
    - "Переключение класса 1→2→3 множит трудозатраты на ×1.0/×1.2/×1.4 (только труд, не материалы)"
    - "Стоимость нанесения нелинейна по толщине (первые 3 мм по 900, далее по 500)"
    - "Результат содержит текстовую оговорку про деталь до 80 кг (CALC-07)"
    - "node calculator/engine.selftest.js завершается без ошибки и печатает PASS по всем примерам"
  artifacts:
    - path: "calculator/engine.js"
      provides: "Чистая функция calc(inputs, PARAMS) -> breakdown — ядро движка"
      contains: "export function calc"
      min_lines: 70
    - path: "calculator/engine.selftest.js"
      provides: "Самопроверка на 3 контрольных примерах эталона + assert"
      contains: "137600"
      min_lines: 30
  key_links:
    - from: "calculator/engine.js"
      to: "calculator/params.js"
      via: "import { PARAMS } (params передаётся аргументом)"
      pattern: "export function calc"
    - from: "calculator/engine.selftest.js"
      to: "calculator/engine.js"
      via: "import { calc }"
      pattern: "import.*calc.*engine"
---

<objective>
Реализовать ядро движка — чистую функцию `calc(inputs, PARAMS) -> breakdown`, число-в-число
воспроизводящую эталон «Калькуляция МультиКрит 700.xlsx», и файл самопроверки на контрольных
примерах. Движок по входам S (площадь, м²), T (толщина, мм), klass (1/2/3) выдаёт полную разбивку
(трудозатраты построчно, ЕСН, вспомогательные, склад, накладные, прибыль, материалы, гарантия,
ИТОГО, ₽/м², ₽/(м²·мм)) плюс список текстовых оговорок (включая оговорку про деталь до 80 кг).

Назначение: это вычислительное сердце калькулятора. UI этапа 2 просто вызовет `calc()` и отрисует
`breakdown`. Главный критерий успеха — совпадение с эталоном (CALC-01).
Выход: `calculator/engine.js` (ядро) и `calculator/engine.selftest.js` (3 контрольных примера).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-calc-engine/01-CONTEXT.md
@.planning/phases/01-calc-engine/01-RESEARCH.md
@.planning/phases/01-calc-engine/01-VALIDATION.md
@calculator/params.js
@CLAUDE.md

<interfaces>
<!-- Контракт PARAMS из плана 01. Движок ссылается только на P.xxx — без магических чисел. -->
<!-- Точные имена полей берутся из реального calculator/params.js (план 01). Здесь — ожидаемый смысл. -->

calculator/params.js экспортирует:
  export const PARAMS = Object.freeze({ ... })

Ключевые группы полей (имена уточнить по факту в params.js):
  - физика/нормы: расход эластомера (1.25), потери (0.10), нормы грунтов/очистителя, потери грунтов/очистителя
  - цены материалов: эластомер по порогам 6800/6650/6400, грунты 7700, очиститель 980
  - ставки работ: дробеструйка 1850, обеспыливание 80, обезжиривание 80, грунтование 150,
    rasc1mm 900, rascPosled 500, ставка н/ч 450, доли 0.4/0.3, ИТР 4500
  - проценты: esn 30.2, sklad 1.5, nakladnye 43, pribyl 60, garantiya 5, nds 0
  - коэффициенты класса: 1.0 / 1.2 / 1.4
  - цены вспомогательных + флаги
  - фиксы CALC-08: дни 1, ИТР 1, спец 1, флаги транспорт/командировки/оснастка/дилер = 0
  - tekstOgovorki80kg: строка оговорки про деталь до 80 кг
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Ядро calc() в calculator/engine.js — материал, труд, накрутки, итог</name>
  <files>calculator/engine.js</files>
  <read_first>
    - .planning/phases/01-calc-engine/01-RESEARCH.md — разделы «Полная цепочка расчёта (эталон 700, адреса ячеек)» (шаги 1–4), «Архитектура движка» (паттерн 1, схема потока), «Подводные камни» (камни 1–3), «Не реализовывать вручную». Формулы переносить РОВНО в порядке Excel.
    - calculator/params.js — реальные имена полей PARAMS (создан в плане 01).
    - .planning/phases/01-calc-engine/01-CONTEXT.md — решения (коэффициент класса только к труду; маржу на материал не добавлять; порядок накруток; гарантия после прибыли).
    - CLAUDE.md — имена файлов латиницей; русские комментарии.
  </read_first>
  <behavior>
    - calc({S:1.6,T:5,klass:1}, PARAMS) → breakdown.itogo ≈ 137600.16, breakdown.perM2 ≈ 86000.10, breakdown.perM2mm ≈ 17200.02
    - Трудозатраты (G56) при классе 1 ≈ 19035.24; материалы (G86) = 80792
    - Нелинейность нанесения: внутренняя расценка F52(T=2)=1800, F52(T=3)=2700, F52(T=5)=3700 (излом на 3 мм)
    - Ценовой порог эластомера: масса <20 → 6800, 20..100 → 6650, ≥100 → 6400 (по округлённой вверх массе)
    - Коэффициент класса умножает только SUM(работ), НЕ ИТР и НЕ материалы: формула (sumWorks*coef + ITR), не (sumWorks+ITR)*coef
    - Округление: ТОЛЬКО там, где Excel (Math.ceil для ROUNDUP массы до целого, грунтов/очистителя до 0.1); промежуточные — полный double
  </behavior>
  <action>
    Создать `calculator/engine.js` с единственным экспортом `export function calc(inputs, P)`, где
    `inputs = { S, T, klass }`, `P` — объект PARAMS. Функция чистая: без DOM, без чтения глобалей,
    без даты/random, детерминирована. Перенести цепочку эталона строго по порядку (RESEARCH «Полная
    цепочка расчёта»):

    1) Коэффициент класса coef из P (klass 3→1.4, 2→1.2, иначе 1.0).
    2) Материал: объём эластомера Vэл = S*10000*T/10; масса = Vэл*расход/1000*(1+потери);
       E80 = Math.ceil(масса) (ROUNDUP до целого); цена эластомера по порогу E80 (6800/6650/6400);
       грунт предв./осн./очиститель с ROUNDUP до 0.1 (Math.ceil(x*10)/10) × цены; основные материалы
       G86 = сумма (эластомер + грунты + очиститель). Грунт по резине и металлополимер = 0 (флаги).
    3) Труд построчно (G42..G53): дробеструйка, обеспыливание, подготовка ДО (S*0.4*450),
       обезжиривание (S*80), грунт-работы предв./осн. (S*150 каждая), нанесение S*F52(T) где
       F52 = T<=3 ? T*rasc1mm : (T-3)*rascPosled + 3*rasc1mm, подготовка ПОСЛЕ (S*0.3*450);
       sumWorks = сумма строк; ИТР = 4500*дни; ЕСН = (sumWorks*coef + ИТР)*esn/100;
       трудозатраты G56 = sumWorks*coef + ИТР + ЕСН.
    4) Вспомогательные материалы G62: посчитать количества расходников (гибрид масса E80/площадь S
       как в «700» per CONTEXT — нормативы из RESEARCH «Нормативы вспомогательных материалов»),
       стоимость = кол-во×цена, итог = сумма (per CALC-05; «баг сдвига» — ложная тревога, формула
       корректна). Для канона G62 ≈ 2933.58.
    5) Накрутки строго в порядке: склад G63 = (G62+G86)*sklad/100; накладные G64 = G56*nakladnye/100;
       транспорт/командировки = 0 (CALC-08); G74 = G64+G62+G56+транспорт+командировки+G63;
       прибыль G75 = (G74-командировки)*pribyl/100; ИТОГО работы G77 = G74+G75.
    6) Итог: гарантия G87 = (G77+G86)*garantiya/100 (ПОСЛЕ прибыли, от работы+материалы — камень 3);
       G88 = G77+G86+G87; НДС G89 = G88*nds/100 = 0; ИТОГО G90 = G88+G89;
       perM2 = G90/S; perM2mm = perM2/T.

    Вернуть объект `breakdown` с полями: построчные трудозатраты, sumWorks, esn, trudozatraty (G56),
    vspomMaterialy (G62), sklad, nakladnye, pribyl, rabotyItogo (G77), materialy (G86), garantiya (G87),
    itogo (G90), perM2, perM2mm — и поле `ogovorki: []` (массив строк, наполняется в задаче 2).
    Все числа — через `P.xxx`, никаких магических чисел в формулах (CALC-09, антипаттерн RESEARCH).
    НЕ помещать полный листинг в этот план — реализацию писать по RESEARCH.
  </action>
  <verify>
    <automated>node -e "import('./calculator/engine.js').then(async e=>{const {PARAMS}=await import('./calculator/params.js'); const r=e.calc({S:1.6,T:5,klass:1},PARAMS); const near=(a,b)=>Math.abs(a-b)<=0.05; if(!near(r.itogo,137600.16)) throw new Error('itogo='+r.itogo); if(!near(r.perM2,86000.10)) throw new Error('perM2='+r.perM2); console.log('CALC-01 OK itogo='+r.itogo.toFixed(2));})"</automated>
  </verify>
  <acceptance_criteria>
    - `calculator/engine.js` существует, имя латиницей, экспортирует `calc`.
    - `calc({S:1.6,T:5,klass:1}, PARAMS).itogo` равно 137600.16 ± 0.05.
    - `.perM2` равно 86000.10 ± 0.05; `.perM2mm` ≈ 17200.02.
    - Внутренняя расценка нанесения нелинейна: при T=2 даёт 1800, при T=5 — 3700 (излом на 3 мм).
    - Коэффициент класса применён только к sumWorks (не к ИТР, не к материалам).
    - Никаких магических чисел в формулах — все ставки/проценты через P.xxx.
    - Команда verify печатает `CALC-01 OK` и завершается без ошибки.
  </acceptance_criteria>
  <done>
    Ядро `calc()` воспроизводит канонический пример эталона (137 600,16) до округления; verify печатает
    `CALC-01 OK`.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Текстовая оговорка про 80 кг и сборка списка оговорок (CALC-07, CALC-08)</name>
  <files>calculator/engine.js</files>
  <read_first>
    - calculator/engine.js — текущий код ядра (задача 1).
    - calculator/params.js — поле tekstOgovorki80kg и фиксы CALC-08.
    - .planning/phases/01-calc-engine/01-CONTEXT.md — раздел «Флаг деталь тяжелее 80 кг»: НЕ считается строкой, только текстовая оговорка.
    - .planning/REQUIREMENTS.md — формулировка CALC-07.
  </read_first>
  <action>
    Дополнить `calc()` в `calculator/engine.js`: наполнить `breakdown.ogovorki` массивом текстовых
    оговорок. Обязательно добавить оговорку про деталь до 80 кг (взять готовую строку
    `P.tekstOgovorki80kg`): «Расчёт сделан для детали массой до 80 кг. При большей массе добавится
    грузоподъёмное оборудование и итоговая стоимость вырастет.» (per CALC-07 — отдельная расчётная
    строка НЕ считается, грузоподъёмное оборудование не входит в сумму).

    Добавить также оговорки фиксированных допущений (CALC-08): расчёт на 1 рабочий день, 1 специалист,
    без транспорта, без командировок, без спецоснастки. Оговорки — короткие строки на русском, готовые
    к показу в UI этапа 2. Флаг heavy/«>80 кг» во входы движка НЕ добавлять (убран per CONTEXT).
    Из входов `calc()` остаются только S, T, klass.
  </action>
  <verify>
    <automated>node -e "import('./calculator/engine.js').then(async e=>{const {PARAMS}=await import('./calculator/params.js'); const r=e.calc({S:1.6,T:5,klass:1},PARAMS); if(!Array.isArray(r.ogovorki)) throw new Error('ogovorki не массив'); const has80=r.ogovorki.some(s=>/80\s*кг/.test(s)); if(!has80) throw new Error('нет оговорки про 80 кг'); console.log('CALC-07 OK, оговорок: '+r.ogovorki.length);})"</automated>
  </verify>
  <acceptance_criteria>
    - `breakdown.ogovorki` — массив строк.
    - Содержит оговорку про деталь до 80 кг (регэксп `/80\s*кг/` находит совпадение).
    - Содержит оговорки фиксированных допущений (1 день, 1 спец, без транспорта/командировок/оснастки).
    - Входы `calc()` остаются {S, T, klass} — флага heavy нет.
    - Команда verify печатает `CALC-07 OK` и завершается без ошибки.
  </acceptance_criteria>
  <done>
    Результат `calc()` содержит текстовую оговорку про 80 кг и фикс-допущения; verify печатает
    `CALC-07 OK`.
  </done>
</task>

<task type="auto">
  <name>Задача 3: Самопроверка calculator/engine.selftest.js — 3 контрольных примера</name>
  <files>calculator/engine.selftest.js</files>
  <read_first>
    - .planning/phases/01-calc-engine/01-VALIDATION.md — стратегия самопроверки, Per-Task Verification Map.
    - .planning/phases/01-calc-engine/01-RESEARCH.md — раздел «Контрольные примеры (CALC-01)» (таблица с числами по классам 1/2/3) и паттерн 3 «Самопроверка без фреймворка».
    - .planning/phases/01-calc-engine/01-CONTEXT.md — раздел «Контрольные примеры» (137 600,16 / 143 931,08 / 150 262,00).
    - calculator/engine.js — сигнатура calc и форма breakdown.
  </read_first>
  <action>
    Создать `calculator/engine.selftest.js` — самопроверку без тест-фреймворка (голый JS,
    `assertEqual` через сравнение с округлением). Импортировать `calc` из `./engine.js` и `PARAMS`
    из `./params.js`. Завести массив контрольных примеров (per CONTEXT/RESEARCH, гарантия 5%):

      • S=1.6, T=5, класс 1 → ИТОГО 137 600.16, ₽/м² 86 000.10
      • S=1.6, T=5, класс 2 → ИТОГО 143 931.08, ₽/м² 89 956.92
      • S=1.6, T=5, класс 3 → ИТОГО 150 262.00, ₽/м² 93 913.75

    Для каждого примера: вызвать calc, сравнить itogo и perM2 с ожидаемым с допуском eps (например
    0.05 на округление), напечатать PASS/FAIL и фактические числа. Добавить отдельные проверки:
    нелинейность нанесения (внутренняя расценка при T=2 = 1800, T=5 = 3700) и кратность класса (труд
    при классе 2/3 относительно класса 1 даёт указанные ИТОГО). Если хоть один пример FAIL —
    `process.exitCode = 1` (в браузере просто красный лог). Файл должен запускаться командой
    `node calculator/engine.selftest.js` и работать как ES-модуль.
  </action>
  <verify>
    <automated>node calculator/engine.selftest.js</automated>
  </verify>
  <acceptance_criteria>
    - `calculator/engine.selftest.js` существует, запускается через `node calculator/engine.selftest.js`.
    - Все 3 контрольных примера печатают PASS (ИТОГО 137 600.16 / 143 931.08 / 150 262.00 совпадают в пределах округления).
    - Прогон проверяет нелинейность нанесения и кратность класса.
    - Команда завершается с кодом 0 (нет FAIL).
  </acceptance_criteria>
  <done>
    `node calculator/engine.selftest.js` печатает PASS по всем трём примерам и завершается без ошибки —
    движок численно сверен с эталоном (CALC-01 закрыт).
  </done>
</task>

</tasks>

<threat_model>
## Границы доверия

| Граница | Описание |
|---------|----------|
| (минимальная) | Слой — чистое вычисление без сети, аутентификации и хранилища. Недоверенный пользовательский ввод (валидация полей) приходит на этапе 2 (UI). Здесь — только защита функции от мусорных входов на уровне разумного поведения. |

## Реестр угроз STRIDE

| Threat ID | Категория | Компонент | Disposition | План митигации |
|-----------|-----------|-----------|-------------|----------------|
| T-01-03 | Denial of Service | calc() с мусорным вводом (NaN, отрицательные S/T) | accept | На этапе 1 валидация ввода вне области (UI этапа 2). Функция чистая и синхронная, бесконечных циклов нет; мусорный ввод даёт NaN, не вешает процесс. High-угроз на слое нет. |
| T-01-04 | Tampering | подмена PARAMS при вызове calc | mitigate | PARAMS заморожен в плане 01 (Object.freeze); calc не мутирует входной P. |
</threat_model>

<verification>
- `node calculator/engine.selftest.js` — все 3 примера PASS, код выхода 0.
- `calc({S:1.6,T:5,klass:1}, PARAMS).itogo` ≈ 137 600.16 (CALC-01).
- Переключение класса 1→2→3 даёт 137 600.16 / 143 931.08 / 150 262.00 (CALC-02).
- Внутренняя расценка нанесения: T=2→1800, T=5→3700 (CALC-03).
- `breakdown.ogovorki` содержит оговорку про 80 кг (CALC-07).
- Нет магических чисел в формулах — всё через PARAMS (CALC-09 соблюдён движком).
</verification>

<success_criteria>
- Движок `calc()` воспроизводит эталон «700» число-в-число на 3 контрольных примерах (CALC-01).
- Класс 1/2/3 множит трудозатраты ×1.0/×1.2/×1.4, только труд (CALC-02).
- Нанесение нелинейно по толщине (CALC-03).
- Материалы по S и T с ценовыми порогами по массе (CALC-04).
- Вспомогательные материалы воспроизводят итог эталона (CALC-05).
- Порядок накруток как в эталоне, гарантия после прибыли, НДС=0 (CALC-06).
- Результат содержит текстовую оговорку про деталь до 80 кг (CALC-07).
- `node calculator/engine.selftest.js` зелёный.
</success_criteria>

<output>
После завершения создать `.planning/phases/01-calc-engine/01-02-SUMMARY.md`.
</output>
