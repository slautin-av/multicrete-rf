---
phase: 01-calc-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - calculator/params.js
autonomous: true
requirements: [CALC-08, CALC-09]
must_haves:
  truths:
    - "Все числа эталона (ставки, цены, нормы, проценты, коэффициенты класса) лежат в одном объекте PARAMS"
    - "PARAMS заморожен (Object.freeze) и экспортируется как ES-модуль"
    - "Фиксированные допущения (1 день, 1 спец, без транспорта/командировок/оснастки) зашиты значениями по умолчанию"
  artifacts:
    - path: "calculator/params.js"
      provides: "Единый источник истины: все коэффициенты/ставки/цены/нормы эталона 700"
      contains: "export const PARAMS = Object.freeze"
      min_lines: 40
  key_links:
    - from: "calculator/params.js"
      to: "calculator/engine.js"
      via: "import { PARAMS }"
      pattern: "export const PARAMS"
---

<objective>
Создать единый замороженный блок параметров `PARAMS` — один источник истины для всего движка
(CALC-09). Сюда переносятся ВСЕ числа эталона «Калькуляция МультиКрит 700.xlsx»: нормы и физика
материала, цены материалов, ставки работ, проценты-накрутки, коэффициенты класса, нормативы
вспомогательных материалов и фиксированные допущения (CALC-08).

Назначение: после этого плана движок (план 02) ссылается только на `P.xxx` — никаких «магических
чисел» в формулах. Поменять ставку/процент можно одним числом в одном месте.
Выход: `calculator/params.js` с `export const PARAMS = Object.freeze({...})`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-calc-engine/01-CONTEXT.md
@.planning/phases/01-calc-engine/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Создать calculator/params.js — единый блок PARAMS</name>
  <files>calculator/params.js</files>
  <read_first>
    - .planning/phases/01-calc-engine/01-RESEARCH.md — разделы «Полный список параметров (PARAMS — CALC-09)», «Полная цепочка расчёта», «Нормативы вспомогательных материалов». Все значения берутся ОТСЮДА.
    - .planning/phases/01-calc-engine/01-CONTEXT.md — зафиксированные решения (гарантия 5% по умолчанию, маржу на материал не добавляем, класс ×1.0/×1.2/×1.4).
    - CLAUDE.md — правила (русский; имена файлов кода латиницей и дефисами; числа калькулятора публичны по решению владельца — это НЕ секреты).
  </read_first>
  <action>
    Создать файл `calculator/params.js` (латиница, без кириллицы в имени). В нём — единственный
    экспорт: `export const PARAMS = Object.freeze({ ... })`. Сгруппировать поля с русскими
    комментариями по разделам RESEARCH:

    1) Нормы и физика материала (лист РП): расход эластомера 1.25, тех. потери эластомера 0.10,
       норма предв. грунта 0.13, норма осн. грунта 0.17, норма очистителя 0.75, тех. потери
       предв./осн. грунта 0.05/0.05, тех. потери очистителя 0.10.
    2) Цены материалов (кэш-значения прайса 2 кв. 2024 — пометить дату комментарием): эластомер
       по порогам 6800 (<20 кг) / 6650 (20–100 кг) / 6400 (≥100 кг), цена грунтов 7700,
       цена очистителя 980.
    3) Ставки работ: дробеструйка 1850, обеспыливание 80, обезжиривание 80, предв. грунтование 150,
       осн. грунтование 150, расценка нанесения первые 3 мм 900, расценка нанесения последующие 500,
       ставка н/ч подготовки 450, доля н/ч подготовки ДО 0.4, доля н/ч подготовки ПОСЛЕ 0.3,
       ставка ИТР 4500.
    4) Проценты-накрутки: ЕСН 30.2, складские 1.5, накладные 43, плановые накопления (прибыль) 60,
       гарантийный резерв 5 (по умолчанию 5% per CONTEXT; добавить комментарий «легко переключить
       на 3»), НДС 0.
    5) Коэффициенты класса: класс 1 → 1.0, класс 2 → 1.2, класс 3 → 1.4 (per CALC-02; применяются
       к трудозатратам).
    6) Нормативы и цены вспомогательных материалов (лист ВМ, гибрид масса/площадь как в «700» per
       CONTEXT): сопло цена 450, кисти 60мм 63, кисти радиаторные 90, стаканчики 0.8, ветошь 400,
       полотенца 90, перчатки 490, костюм 760, фильтр респиратора 1260, диск зачистной 160,
       плёнка 1250, скотч 250, растворитель промывки 200 (фикс кол-во 8), оснастка 0 (CALC-08 — без
       оснастки), альфа-диск/алмазный диск выкл (0). Нормативы количества НЕ зашивать сюда — они
       зависят от S и массы и вычисляются в engine.js; в PARAMS только ЦЕНЫ за единицу + флаги вкл/выкл.
    7) Фиксированные допущения (CALC-08): дни 1, ИТР-чел 1, спец-чел 1, флаг транспорта 0,
       флаг командировок 0, флаг оснастки 0, флаг дилера 0.
    8) Текстовая оговорка про 80 кг — НЕ сюда (она нужна движку как готовая строка; вынести в PARAMS
       как поле `tekstOgovorki80kg` со строкой: «Расчёт сделан для детали массой до 80 кг. При большей
       массе добавится грузоподъёмное оборудование и итоговая стоимость вырастет.»).

    Имена полей — латиницей (транслит или англ.), компактно, чтобы движок ссылался как `P.esn`,
    `P.cenaEl_do20`, `P.rasc1mm` и т.д. Никаких внешних путей к прайсу — только числа-кэш.
    Файл не содержит формул и логики — только данные. НЕ помещать листинги кода в этот план,
    структуру полей подобрать по RESEARCH.
  </action>
  <verify>
    <automated>node -e "import('./calculator/params.js').then(m=>{const P=m.PARAMS; if(!Object.isFrozen(P)) throw new Error('PARAMS не заморожен'); const need=['garantiya','esn','nakladnye','sklad','pribyl','rasc1mm','rascPosled']; for(const k of need){if(P[k]===undefined) throw new Error('нет поля '+k);} if(P.garantiya!==5) throw new Error('гарантия по умолчанию должна быть 5'); if(typeof P.tekstOgovorki80kg!=='string'||!/80\s*кг/.test(P.tekstOgovorki80kg)) throw new Error('нет текста оговорки про 80 кг'); console.log('PARAMS OK');})"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `calculator/params.js` существует, имя латиницей.
    - Экспортирует `PARAMS` через `Object.freeze` (проверка `Object.isFrozen` проходит).
    - Присутствуют ключевые поля процентов и расценок (esn, nakladnye, sklad, pribyl, garantiya, rasc1mm, rascPosled).
    - `garantiya` по умолчанию = 5 (per CONTEXT).
    - Есть строковое поле с текстом оговорки про деталь до 80 кг.
    - Никаких внешних путей/ссылок на прайс-файл, только числовые значения.
    - Команда `node -e "..."` из verify печатает `PARAMS OK` и завершается без ошибки.
  </acceptance_criteria>
  <done>
    `calculator/params.js` создан, заморожен, содержит все числа эталона одним блоком, verify-команда
    печатает `PARAMS OK`.
  </done>
</task>

</tasks>

<threat_model>
## Границы доверия

| Граница | Описание |
|---------|----------|
| (нет) | На этом слое нет сети, аутентификации, хранилища. `params.js` — статический словарь чисел, публичный по решению владельца. Недоверенный ввод (валидация полей) приходит на этапе 2 (UI). |

## Реестр угроз STRIDE

| Threat ID | Категория | Компонент | Disposition | План митигации |
|-----------|-----------|-----------|-------------|----------------|
| T-01-01 | Information Disclosure | calculator/params.js (числа цен/ставок) | accept | Числа публичны намеренно (решение владельца 2026-06-29): прозрачный калькулятор, коэффициенты не прячем. Это не секреты — правило CLAUDE.md о паролях/ключах не нарушается. |
| T-01-02 | Tampering | PARAMS изменён в рантайме | mitigate | `Object.freeze(PARAMS)` — защита от случайной мутации значений в коде движка. |
</threat_model>

<verification>
- `node -e "..."` (см. verify задачи) печатает `PARAMS OK`.
- В файле нет абсолютных путей к внешнему прайс-файлу.
- В файле нет паролей/ключей/токенов (числа калькулятора публичны — не секреты).
</verification>

<success_criteria>
- `calculator/params.js` существует и экспортирует замороженный `PARAMS`.
- Все коэффициенты/ставки/цены/нормы эталона собраны в одном блоке (CALC-09).
- Фиксированные допущения зашиты по умолчанию (CALC-08).
- Гарантийный резерв по умолчанию = 5 % (per CONTEXT, переключаемо одним числом).
</success_criteria>

<output>
После завершения создать `.planning/phases/01-calc-engine/01-01-SUMMARY.md`.
</output>
